Distributed Systems Final — Redis Cluster + CRUD API + Leaderboard

This project is a full distributed system built using:

Redis Cluster (3 nodes) deployed in Docker

Custom Node.js API with CRUD operations

Distributed leaderboard using Redis Sorted Sets

Optional HTML frontend

It demonstrates distributed key-value storage, failover behavior, scalable leaderboard functionality, and API integration with a running Redis Cluster.
